# Port the gateway listens on
PORT=3001

# URL of the auth-service (use service name when running via Docker Compose)
AUTH_SERVICE_URL=http://localhost:3000

# Path on auth-service for OpenAPI JSON (gateway merges under /auth). Default tries /openapi.json then /documentation/json
AUTH_OPENAPI_PATH=/openapi.json
