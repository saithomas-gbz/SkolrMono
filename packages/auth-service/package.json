{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun --watch run src/app.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external @prisma/client --external .prisma/client",
    "start": "bun run ./dist/app.js",
    "prisma:generate": "bunx prisma generate",
    "prisma:migrate": "bunx prisma migrate dev"
  },
  "keywords": [],
  "author": "thomas.grare",
  "license": "ISC",
  "devDependencies": {
    "@prisma/adapter-pg": "^7.5.0",

    "dotenv": "^17.2.4",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/jwt": "^10.0.0",
    "@fastify/oauth2": "^8.1.2",
    "@prisma/client": "^7.5.0",
    "bcrypt": "^6.0.0",
    "pg": "^8.20.0"
  }
}
